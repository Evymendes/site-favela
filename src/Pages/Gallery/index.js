import Layout from '../Layout';

import * as S from './styles';
  
function Gallery() {
  return (
    <Layout>
      <S.BackText>
        Por enquanto, veja a preparação para o lançamento:
      </S.BackText>
    </Layout>
  );
}
  
export default Gallery;
  

